apiVersion: v1
kind: Service
metadata:
  name: {{ include "bracket-service.fullname" . }}
  labels:
    {{- include "bracket-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bracket-service.selectorLabels" . | nindent 4 }}