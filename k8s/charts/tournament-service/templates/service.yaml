apiVersion: v1
kind: Service
metadata:
  name: {{ include "tournament-service.fullname" . }}
  labels:
    {{- include "tournament-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tournament-service.selectorLabels" . | nindent 4 }}
