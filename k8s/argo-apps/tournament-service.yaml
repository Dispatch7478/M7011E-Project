apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tournament-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/dispatch7478/m7011e-project.git
    targetRevision: HEAD
    path: k8s/charts/tournament-service
  destination:
    server: https://kubernetes.default.svc
    namespace: t-hub-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
